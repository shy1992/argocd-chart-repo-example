apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cnpg
spec:
  project: default
  source:
    repoURL: https://github.com/shy1992/argocd-chart-repo-example
    targetRevision: HEAD
    path: charts/cnpg
    helm:
      valueFiles:
        - ../../environments/dev/cnpg-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: database
  syncPolicy:
    automated: {}
    syncOptions:
      - CreateNamespace=true
